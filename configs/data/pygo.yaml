_target_: src.data.gosyn.GoSynDataModule
data_dir: "/media/michael/Data1/dev/pygo_synthetic/renders_new"
batch_size: 4 # Needs to be divisible by the number of devices (e.g., if in a distributed setup)
train_val_test_split: [8000, 1000, 1000]
num_workers: 1
pin_memory: true