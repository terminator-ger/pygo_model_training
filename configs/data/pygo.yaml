_target_: src.data.gosyn.GoSynDataModule
data_dir: "E:/dev/pygo_synthetic/renders_new"
batch_size: 2 # Needs to be divisible by the number of devices (e.g., if in a distributed setup)
train_val_test_split: [80, 10, 10]
num_workers: 1
pin_memory: True