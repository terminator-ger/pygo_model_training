# @package _global_

# to execute this experiment run:
# python train.py experiment=example

defaults:
  - override /data: pygo 
  - override /model: pygo
  - override /callbacks: default
  - override /trainer: default

# all parameters below will be merged with parameters from default configurations set above
# this allows you to overwrite only specified parameters

tags: ["pygo", "mobilenetv3_mh_large_150d"]

seed: 12345

trainer:
  min_epochs: 10
  max_epochs: 10
  gradient_clip_val: 0.5

model:
  optimizer:
    lr: 0.002
  net:
    name: "mobilenetv3_mh_large_150d"
    pretrained: true
  compile: false

data:
  batch_size: 64

logger:
  wandb:
    tags: ${tags}
    group: "pygo"
  aim:
    experiment: "pygo"
